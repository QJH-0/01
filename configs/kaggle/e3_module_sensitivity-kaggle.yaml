# Kaggle 占位配置（与 configs/e3_module_sensitivity.yaml 对齐）。
# 计算设备说明：占位；意图 GPU 训练。接入后 runtime.device 建议 auto 或 cuda（见 configs/kaggle/e1_teacher_smoke_train-kaggle.yaml）。

experiment:
  name: e3_module_sensitivity

model:
  teacher_checkpoint: /kaggle/working/checkpoints/e1_teacher.pth
  target_module: tcn

output:
  root_dir: /kaggle/working/Experiments

notes: Placeholder config for E3.
